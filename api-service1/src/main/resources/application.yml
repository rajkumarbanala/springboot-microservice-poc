server:
  port: 9001

spring:
  application:
    name: api-service1

#eureka
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9004/eureka/
  instance:
    prefer-ip-address: false
